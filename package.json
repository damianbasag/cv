{
  "name": "damian-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio Damiana Basąga",
  "scripts": {
    "build": "node build.js && tailwindcss -i input.css -o dist/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
